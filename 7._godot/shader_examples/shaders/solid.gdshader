shader_type canvas_item;

// corre para cada pixel
void fragment() {
	// COLOR es una variable de salida, el color final del pixel
	// COLOR es un vec4 (red, green, blue, alpha)
	COLOR = vec4(1.0, 0.7, 0.0, 0.5);
}
